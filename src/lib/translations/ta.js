/**
 * Wave XII-1 (PRODUCTION.md): Tamil string map — PLACEHOLDER.
 *
 * USER SUPPLIES: replace the empty object below with the Tamil string map.
 * Keys must mirror src/lib/translations/en.js 1:1 (flat, stable, one level
 * only) so this file is a pure string-map swap — no code changes needed.
 *
 * Font note: Tamil needs a system/fallback font stack (Fraunces/Geist are
 * Latin-only). Add the fallback stack to the CSS font-family declarations
 * before shipping Tamil.
 */
export default {}